// Constants `screens`, `actions` and `reducer` are exported via named export
// It is important to use those exact names

import WordpressFeedPage from './pages/wordpress-feed-page';
import reducer from './redux';

export const pages = {
  WordpressFeedPage,
};

export { reducer };
