import { screens as navigationScreens } from 'shoutem.navigation';

const { IconGrid, List, CardList } = navigationScreens;

export const screens = {
  List,
  IconGrid,
  CardList,
};

export const actions = {};

export const reducer = {};

